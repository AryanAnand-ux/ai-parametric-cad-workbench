# Optional backend URL for production deployments. Leave blank for Vite proxy/same-origin.
VITE_API_URL=
# Long-running generation and chat-to-modify timeout in milliseconds.
VITE_GENERATION_TIMEOUT_MS=300000
