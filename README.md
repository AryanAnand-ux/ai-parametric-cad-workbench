# AI-Driven Parametric CAD Workbench

> **Natural Language → 3D Solid Model.** Type a mechanical part description, get an interactive 3D model in your browser, tune it live with real-time sliders, refine it conversationally with Chat-to-Modify, and export production-ready STL & STEP files.

---

## ⚡ Quick Start (Run Locally in 2 Steps)

### 1. Backend Setup (FastAPI + build123d + RAG)
```bash
# Navigate to backend
cd backend

# Create & activate virtual environment
python -m venv venv
# Windows (PowerShell):
.\venv\Scripts\Activate.ps1
# Windows (cmd):
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

# Install dependencies
pip install -r requirements.txt

# Configure API Keys (Copy .env.example)
copy .env.example .env     # Windows
# cp .env.example .env       # Linux/macOS
```

> **Add your API Key in `.env`:**
> `GEMINI_API_KEY=your_key_here` (Free from [Google AI Studio](https://aistudio.google.com/apikey))  
> `GROQ_API_KEY=your_key_here` *(optional fallback)*

Run pre-flight check & start server:
```bash
python startup_check.py
python -m uvicorn main:app --reload --port 8000
```
*Backend runs on: **http://127.0.0.1:8000** | Interactive Swagger Docs: **http://127.0.0.1:8000/docs***

---

### 2. Frontend Setup (React + Vite + Three.js)
Open a new terminal:
```bash
# Navigate to frontend
cd frontend

# Install node dependencies
npm install

# Start Vite dev server
npm run dev
```
*Frontend runs on: **http://localhost:5173***

---

## 🌟 Key System Features

- 🧊 **Real CAD Solid Engine (`build123d` + OpenCASCADE)**: Generates true boundary-representation (B-Rep) solid models with exact CSG operations, fillets, chamfers, and STEP/STL export.
- 🧠 **50-Example RAG Vector Store**: Uses local `sentence-transformers/all-MiniLM-L6-v2` embeddings in ChromaDB to retrieve top-3 CAD code examples for few-shot LLM prompt injection.
- 💬 **Chat-to-Modify (`POST /api/modify`)**: Conversationally refine generated models with natural language (e.g., *"Make the walls 2mm thicker"*, *"Add a chamfer to top edges"*) without losing parameter continuity. Creates versioned scripts (`_v1`, `_v2`...).
- ⚡ **Sub-200ms Parametric Recomputation**: Adjusting UI sliders recomputes solid geometry directly via `build123d` in real-time without invoking LLM tokens.
- 💻 **Python Code Inspector**: View, inspect, and copy the raw `build123d` Python script generated by the LLM for any design.
- 🛡️ **AST Security Sandbox**: Whitelists only safe modules (`build123d`, `math`, `typing`, etc.) and isolates subprocess execution in a non-blocking thread pool.
- 🎨 **Soft Neobrutalism UI**: Bold outlines (`2.5px solid #18181B`), hard offset drop-shadows, Space Grotesk typography, and a studio blueprint 3D WebGL viewport powered by React Three Fiber.
- 🔄 **Automated Self-Correction Loop**: Catches script execution errors at runtime and feeds tracebacks back to the LLM (up to 3 retries) for autonomous code repair.

---

## 📁 Repository Structure

```
ai-parametric-cad-workbench/
├── README.md                 ← Project Documentation & Setup Guide
├── WEEKLY_PLAN.md            ← 14-Week Development Roadmap & Milestones
├── backend/
│   ├── main.py               ← FastAPI Application & Route Handlers (/generate, /modify, /recompute)
│   ├── schemas.py            ← Pydantic V2 API Request/Response Schemas
│   ├── config.py             ← Environment Variable & Isolated Directory Configuration
│   ├── startup_check.py      ← Pre-flight Dependency & Vector Index Sanity Check
│   ├── benchmark_eval.py     ← Automated 20-Prompt CAD Benchmark & Telemetry Runner
│   ├── requirements.txt      ← Python Dependencies (build123d, chromadb, fastapi, etc.)
│   ├── test_week4_build123d.py ← Engine & RAG Retrieval Test Suite
│   ├── test_api.py           ← Async API Endpoint Test Suite (Health, Generate, Recompute, Modify)
│   ├── services/
│   │   ├── cad_runner.py     ← Subprocess Executor & AST Security Sandbox (Thread Pool Safe)
│   │   ├── prompts.py        ← System Prompts, RAG Templates, & Chat-to-Modify Instructions
│   │   ├── rag_service.py    ← ChromaDB Indexing, Embedding, & Semantic Retrieval
│   │   ├── llm_service.py    ← 3-Tier LLM Orchestrator (Gemini 2.5 Flash + Fallbacks)
│   │   └── cleanup.py        ← Temporary CAD Artifact Lifecycle Manager
│   └── rag_corpus/
│       ├── examples_week4.py ← First 20 CAD Example Snippets (plates, brackets, tubes)
│       ├── examples_week5.py ← 30 Mechanical CAD Snippets (50 total)
│       └── examples_week8.py ← Complex Multi-Body Parts (chassis, drones, RC vehicles)
└── frontend/
    ├── index.html            ← Main HTML Template with Space Grotesk Google Fonts
    ├── vite.config.js        ← Vite Config with Backend Proxy (/api & /static)
    ├── package.json          ← Frontend Dependencies (@react-three/fiber, drei, three)
    └── src/
        ├── App.jsx           ← Main React App Shell, Chat-to-Modify Panel, & State Management
        ├── index.css         ← Soft Neobrutalism Design System
        ├── api.js            ← Axios Client for Backend Endpoints
        └── components/
            ├── Viewer3D.jsx  ← React Three Fiber WebGL Viewer (Auto-Framing, Wireframe, Lighting)
            └── ParameterSlider.jsx ← Parametric Slider Control with Unit Detection & Reset
```

---

## 🔌 API Endpoints Reference

| Method | Endpoint | Description |
|---|---|---|
| `GET` | `/api/health` | Service health status, storage status, & API key configuration |
| `POST` | `/api/generate` | **Primary:** NL prompt → RAG → LLM → execute → 3D STL + STEP |
| `POST` | `/api/modify` | **Chat-to-Modify:** Refine existing script via natural language prompt |
| `POST` | `/api/recompute` | Fast parametric slider recomputation (sub-200ms, no LLM call) |
| `GET` | `/api/script/{id}` | Retrieve raw generated `build123d` Python script by ID |
| `GET` | `/api/admin/models` | List all stored 3D STL/STEP model artifacts |
| `POST` | `/api/admin/cleanup` | Remove temporary artifacts older than threshold |
| `GET` | `/static/models/{file}` | Serve generated STL preview, STEP export, and Python files |

---

## 🧪 Running Automated Tests & Benchmark

```bash
# 1. Run full unit test suite (20 tests — AST security, schemas, parser, geometry)
cd backend
.\venv\Scripts\python -m pytest test_ast_security.py test_schemas.py test_llm_parser.py test_modify_params.py test_geometry_validation.py -v

# 2. Run build123d CAD Engine & RAG Retrieval Tests
.\venv\Scripts\python test_week4_build123d.py

# 3. Run FastAPI Endpoint Tests (Health, Generate, Recompute, Modify)
.\venv\Scripts\python test_api.py

# 4. Run Automated Benchmark Evaluation (requires backend running)
.\venv\Scripts\python benchmark_eval.py --url http://127.0.0.1:8000

# 5. Run Frontend Production Bundle Build
cd ../frontend
npm run build
```

---

## 👥 Team & Responsibilities

| Role | Primary Focus |
|---|---|
| **Lead (AI/RAG)** | LLM orchestrator, ChromaDB vector store, RAG prompt injection, self-correction loop, Chat-to-Modify engine |
| **Partner 2 (Backend/Geometry)** | FastAPI endpoints, `build123d` kernel integration, AST security sandbox, thread-pool executor, artifact cleanup |
| **Partner 3 (Frontend/WebGL)** | React + Vite architecture, React Three Fiber 3D WebGL viewport, Soft Neobrutalism UI design, parametric sliders |
